<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connexion — Bildup</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Spectral:ital,wght@0,400;0,500;0,600;1,400&family=Hanken+Grotesk:wght@300;400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
  <style>
    body { min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 2rem; }
    .auth-page { position: relative; z-index: 1; width: 100%; max-width: 420px; }
    .auth-card {
      background: var(--glass-bg);
      border: 1px solid var(--glass-border);
      border-radius: 10px;
      padding: 2.5rem 2rem;
      backdrop-filter: blur(24px);
      box-shadow: 0 8px 32px rgba(12, 22, 34, 0.12);
    }
    .auth-logo {
      font-family: 'Spectral', serif;
      font-size: 1.2rem;
      color: var(--text-primary);
      margin-bottom: 0.35rem;
    }
    .auth-tagline {
      font-size: 0.72rem;
      color: var(--gold-dim);
      letter-spacing: 0.08em;
      font-family: 'IBM Plex Mono', monospace;
      margin-bottom: 2rem;
    }
    .auth-headline {
      font-family: 'Spectral', serif;
      font-size: 1.5rem;
      font-weight: 400;
      font-style: italic;
      color: var(--text-primary);
      margin-bottom: 0.4rem;
    }
    .auth-sub {
      font-size: 0.82rem;
      color: var(--text-muted);
      font-weight: 300;
      margin-bottom: 2rem;
    }
    .form-group { margin-bottom: 1.25rem; }
    .form-label {
      display: block;
      font-size: 0.75rem;
      font-weight: 500;
      color: var(--text-secondary);
      margin-bottom: 0.4rem;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      font-family: 'IBM Plex Mono', monospace;
    }
    .form-input {
      width: 100%;
      padding: 0.75rem 1rem;
      background: var(--navy-card);
      border: 1px solid var(--navy-border);
      border-radius: 6px;
      color: var(--text-primary);
      font-size: 0.9rem;
      font-family: 'Hanken Grotesk', sans-serif;
      transition: border-color 0.15s ease;
      outline: none;
    }
    .form-input:focus { border-color: var(--gold); }
    .form-input::placeholder { color: var(--text-muted); }
    .form-error {
      font-size: 0.8rem;
      color: #f87171;
      margin-top: 0.4rem;
      display: none;
    }
    .form-error.visible { display: block; }
    .auth-submit {
      width: 100%;
      padding: 0.85rem 1.5rem;
      background: var(--gold);
      color: var(--navy-deep);
      border: none;
      border-radius: 6px;
      font-size: 0.88rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.15s ease, box-shadow 0.15s ease;
      letter-spacing: 0.02em;
      margin-top: 0.5rem;
    }
    .auth-submit:hover {
      background: var(--gold-bright);
      box-shadow: 0 0 20px rgba(39, 86, 201, 0.25);
    }
    .auth-footer {
      margin-top: 1.5rem;
      text-align: center;
      font-size: 0.82rem;
      color: var(--text-muted);
    }
    .auth-footer a { color: var(--gold); text-decoration: none; font-weight: 500; }
    .auth-footer a:hover { color: var(--gold-bright); }
  </style>
</head>
<body>
  <div class="auth-page">
    <div class="auth-card">
      <div class="auth-logo">Bildup</div>
      <div class="auth-tagline">Intelligence d'acquisition sourcée</div>

      <h1 class="auth-headline">Bon retour</h1>
      <p class="auth-sub">Connectez-vous pour poursuivre votre essai gratuit.</p>

      <form id="login-form" method="post" action="/api/auth/login">
        <div class="form-group">
          <label class="form-label" for="email">E-mail professionnel</label>
          <input class="form-input" type="email" id="email" name="email" placeholder="vous@entreprise.fr" autocomplete="email" required>
        </div>
        <div class="form-group">
          <label class="form-label" for="password">Mot de passe</label>
          <input class="form-input" type="password" id="password" name="password" placeholder="Votre mot de passe" required>
        </div>
        <p class="form-error" id="form-error"></p>
        <button class="auth-submit" type="submit">Se connecter</button>
      </form>

      <div class="auth-footer">
        Pas encore de compte ? <a href="/signup">Démarrer l'essai gratuit</a>
      </div>
    </div>
  </div>

  <script>
    const form = document.getElementById('login-form');
    const formError = document.getElementById('form-error');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      formError.classList.remove('visible');

      const data = {
        email: form.email.value,
        password: form.password.value,
      };

      try {
        const res = await fetch('/api/auth/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data),
          redirect: 'manual',
        });

        if (res.status === 200) {
          window.location.href = '/dashboard';
        } else {
          const body = await res.json().catch(() => ({}));
          formError.textContent = body.error || 'Identifiants invalides. Veuillez réessayer.';
          formError.classList.add('visible');
        }
      } catch (err) {
        formError.textContent = 'Erreur réseau. Vérifiez votre connexion et réessayez.';
        formError.classList.add('visible');
      }
    });
  </script>
</body>
</html>